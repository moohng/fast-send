{
  "name": "fast-send-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "server": "npm run dev --workspace=@fast-send/server",
    "client": "npm run dev --workspace=@fast-send/client",
    "check": "npm run check --workspaces",
    "build": "npm run build --workspaces"
  },
  "dependencies": {
    "better-sqlite3": "12.9.0"
  }
}